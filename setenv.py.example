# Copy into your script's directory as `setenv.py`, set the environment variables and save. The `client.py` script will use this file to connect to Viam.

import os

# These must be set. You can get them from your robot's 'Code sample' tab
robot_api_key = os.environ['ROBOT_API_KEY'] = '<set>'
robot_api_key_id = os.environ['ROBOT_API_KEY_ID'] = '<set>'
robot_address = os.environ['ROBOT_ADDRESS'] = '<set>'